实现gif图的功能
作业1：
	1、鼠标拖动蓝色滑块，内容区域滚动
	2、鼠标放在文字区域与滚动条区域，滚动滚轮都可以滚动
	3、鼠标按下滑块区域，滑块的中心滚动到鼠标按下的位置
	4、鼠标点击滑块区域，滑块滚动一定的距离（距离自己定）
	5、鼠标按下上下箭头，滚动条滚动
	6、鼠标按下滑块或者上下箭头，都变个颜色
	7、注意一个问题，鼠标按下滑块区域，这个时候再去滚动滚轮，要实现正确的效果
	8、具体功能可以参考chrom浏览器的滚动条，注意该用哪些事件
作业2：
	1、鼠标放在方块的边上要显示不同的箭头形状，具体可以参考windows的文件夹，鼠标变换的形状是由样式来控制的。关于需要哪些样式，在下面会给大家列出来
	2、拖动改变大小，也与windows的文件夹是一样的功能，注意有一个最小拖动的大小（宽高自己定）。具体功能参考windows文件夹功能
	3、鼠标方向样式如下：
		/*
		 * 鼠标方向样式（cursor）
		 * 左		w-resize
		 * 上		n-resize
		 * 右		e-resize
		 * 下		s-resize
		 * 
		 * 左上		nw-resize
		 * 右上		ne-resize
		 * 右下		se-resize
		 * 左下		sw-resize
		 */
	
	
	
